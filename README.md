# AutoArch Bootstrap

```bash
sudo pacman -S --noconfirm git
```

```bash
git clone https://github.com/pperezp/autoarch-bootstrap.git && cd autoarch-bootstrap && ./install
```

# Fast installation
```bash
curl --output install https://raw.githubusercontent.com/pperezp/autoarch-bootstrap/master/get && chmod +x install && ./install
```